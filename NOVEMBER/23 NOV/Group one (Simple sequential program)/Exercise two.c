#include <stdio.h>

char toupper(char resp); // Mudar

int main() {
    // Fazer um programa em C que imprime uma tabela com a tabuada de 1 a 9

    /* Organizar (Ver o 'final project')
    int opcao = 0;
      int numero = 0;
          char resp;

      do {
          printf("Tabuada do um: ");
          scanf("%d", &opcao);
          scanf("%d", &numero);

          if (opcao == 1) {
              for (int i = 1; i <= 10; i++)
                  printf("1 x %d = %d\n", i, i);
          } else if (opcao == 2) {
              for (int i = 1; i <= 10; i++)
                  printf("2 x %d = %d\n", i, i);
          }
      }
    while (toupper(resp) == 'S'); // Mudar
      return 0;
      */
}
