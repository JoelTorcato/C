#include <stdio.h>

int main() {
    // Initialize an array with 4 integers
    int numbers[4] = {1, 2, 3, 4};
    int i;

    // Loop through the array and print each element
    for (i = 0; i <= 3; i++) {
        printf("%d\n", numbers[i]);
    }

    return 0;
}